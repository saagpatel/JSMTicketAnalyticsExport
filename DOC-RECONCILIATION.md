# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. No source code was modified and no builds or tests were run. It exists so any reviewer can audit what changed and why.

---

## Per-Claim Findings

### 1. What it is
**Status:** consistent  
**Evidence:** `jsm_export.py:1-311`, `config.py:1-20`, `README.md:1-56` — entry point, instance URL, project key, and description all match the documented narrative. Confirmed as a Python CLI that paginates Jira REST API v3 and writes CSV + JSON + manifest output.

### 2. Current state
**Status:** drifted  
**Evidence:** `git log --all` shows `1c14223 feat: JSM Ticket Analytics Export — all phases complete`; `e3ed413 feat(phase1)`, `38286f9 feat(phase2)` also present. All source modules for Phases 0, 1, and 2 exist and are fully implemented: `field_resolver.py`, `transformer.py` (with SLA extraction), `all_time_appender.py`, `com.saagar.jsm-export.plist`. Test files for all modules present with 63 test functions counted by reading `tests/test_transformer.py`, `tests/test_writer.py`, `tests/test_field_resolver.py`, `tests/test_all_time_appender.py`.

**Changes made:**
- `CLAUDE.md` (portfolio-context section): "**Phase 0: Foundation + Core Export** / See IMPLEMENTATION-ROADMAP.md for full phase details." → "**Phases 0–2 complete.** Core export (Phase 0), custom field resolution + SLA extraction + all-time append (Phase 1), and launchd scheduling + backfill mode (Phase 2) are all implemented. See IMPLEMENTATION-ROADMAP.md for phase details."
- `docs/PORTFOLIO-DISPOSITION.md` (current state paragraph): "Per memory: Phase 0, 48 tests." → "Phases 0–2 all implemented; 63 tests (counted from test files)."
- `docs/PORTFOLIO-DISPOSITION.md` (Reactivation procedure step 6): "expect 48 tests passing" → "expect 63 tests passing"
- `docs/PORTFOLIO-DISPOSITION.md` (Verification posture tip + Last known reference): stale `a93ce13` commit hash updated to `230e50b` (current HEAD)

### 3. Stack
**Status:** consistent  
**Evidence:** `requirements.txt:1-2` — `requests==2.33.0`, `keyring==24.3.0`. README.md and CLAUDE.md correctly document Python 3.11+, `requests`, `keyring`, stdlib `csv`/`json`, and macOS `launchd`. CLAUDE.md's `requests 2.31+` minimum spec is satisfied by the pinned 2.33.0.

### 4. How to run
**Status:** drifted  
**Evidence:** `jsm_export.py:109-140` (`_parse_args`) defines these flags: `--month`, `--since`, `--backfill`, `--dry-run`, `--verbose`. No `--project` or `--output` flags exist. `config.py:6-7` hard-codes the instance URL and output directory (`~/Analytics/JSM/`).

**Change made:**
- `README.md` (Usage section): Replaced `python jsm_export.py --project IT --output output/` and `python jsm_export.py --project IT --output output/ --backfill` (flags that do not exist) with correct invocations using `--month`, `--backfill`, and `--dry-run`.

### 5. Known risks
**Status:** consistent  
**Evidence:** `CLAUDE.md` Gotchas/Known Risks — dynamic field resolution is confirmed in `field_resolver.py:14-50`; keyring-only credential storage confirmed in `jira_client.py:64-92`; retry wrapper in `jira_client.py:95-187`; output-to-`~/Analytics/JSM/` in `config.py:7`. No contradictions found.

### 6. Next move
**Status:** unverifiable  
**Evidence:** CLAUDE.md and PORTFOLIO-DISPOSITION.md "Next Recommended Move" / "Unblock trigger" sections describe operator actions (launchd verification, Keychain rotation cadence, Jira API pagination audit). These are forward-looking operational items; no code evidence can confirm or deny them. Text left unchanged.

---

## Contradictions for Manual Review

**`IMPLEMENTATION-ROADMAP.md:163`** — Lists `requests==2.31.0` in the Dependencies block, but `requirements.txt` pins `requests==2.33.0`. One-line fix: change `requests==2.31.0` to `requests==2.33.0` on line 163 of `IMPLEMENTATION-ROADMAP.md`.

---

## Footer

Run timestamp: 2026-06-02 19:47:06 PDT  
Branch: docs/truth-up-2026-06-02  
HEAD SHA reconciled against: 230e50baf78c5335880b42dba73c93939b057fed
